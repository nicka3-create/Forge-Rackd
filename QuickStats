import React from 'react';
import { Card } from '@/components/ui/card';
import { Zap, Trophy, Dumbbell, TrendingUp } from 'lucide-react';

const stats = [
  { icon: Zap, label: 'Streak', key: 'streak_days', suffix: ' days', color: 'text-primary' },
  { icon: Dumbbell, label: 'Workouts', key: 'total_workouts', suffix: '', color: 'text-accent' },
  { icon: Trophy, label: 'PRs', key: 'total_prs', suffix: '', color: 'text-chart-4' },
];

export default function QuickStats({ profile }) {
  return (
    <Card className="p-4 bg-card border-border">
      <div className="flex items-center gap-2 mb-4">
        <TrendingUp className="w-4 h-4 text-primary" />
        <h3 className="text-sm font-semibold font-heading">Your Stats</h3>
      </div>
      <div className="grid grid-cols-3 gap-3">
        {stats.map(({ icon: Icon, label, key, suffix, color }) => (
          <div key={key} className="text-center">
            <div className={`w-10 h-10 rounded-xl bg-secondary flex items-center justify-center mx-auto mb-2`}>
              <Icon className={`w-4 h-4 ${color}`} />
            </div>
            <p className="text-lg font-bold font-heading">{profile?.[key] || 0}{suffix}</p>
            <p className="text-[10px] text-muted-foreground uppercase tracking-wider">{label}</p>
          </div>
        ))}
      </div>
    </Card>
  );
}
